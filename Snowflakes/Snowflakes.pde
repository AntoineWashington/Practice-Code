

void setup(){
  size(800,600);
  for (int i=0; i<count; i++) {
    sz[i] = random(10, 30);
    x[i] = new PVector(random(width),random(-height,-sz[i]/2);
    v[i] = new PVector(random(-1,1),random(0,1);
    a[i] = new PVector(0, .01);
  }
}

void draw(){
  for (int i=0; i<count
}
